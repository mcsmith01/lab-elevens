/**
 * This is a class that tests the Card class.
 */
public class CardTester {

	/**
	 * The main method in this class checks the Card operations for consistency.
	 *	@param args is not used.
	 */
	public static void main(String[] args) {
		/* *** TO BE IMPLEMENTED IN ACTIVITY 1 *** */
		/* Write code that tests your work here. My
		 * suggestion is:
		 * Create three cards, two of which are the same
		 * Print out the result of calls to suit(), rank(),
		 * and pointValue() for each to ensure the values
		 * are initialize properly
		 * Print out the result of a call to toString() for
		 * one of the cards to ensure it is formatted
		 * correctly
		 * Use the matches() method to compare the two that
		 * are the same and ensure the result is true
		 * Use the matches() method to compare two that are
		 * different and ensure the result is false
		 */
	}
}
